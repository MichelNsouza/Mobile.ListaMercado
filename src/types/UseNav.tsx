import { NavigatorScreenParams } from '@react-navigation/native';

export type RootStackParamList = {
  Home: undefined;
  Lista: undefined; // Se 'Lista' aceitar parâmetros, substitua `undefined` pelo tipo correto
};
